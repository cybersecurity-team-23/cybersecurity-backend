services:
  keycloak:
    image: quay.io/keycloak/keycloak:24.0.4
    ports:
      - "8090:8080"
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    command: start-dev
